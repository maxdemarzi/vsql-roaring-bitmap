# Test roaring64_union for ROARING64 custom type

INSTALL EXTENSION vsql_roaring_bitmap;
SELECT roaring64_union(
  ROARING64::from_string('{1,2,3}'),
  ROARING64::from_string('{3,4,5}')
) AS union_bitmap;
UNINSTALL EXTENSION vsql_roaring_bitmap;
